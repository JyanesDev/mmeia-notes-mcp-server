# Decisiones (ADR del proyecto)

Pendiente — se documentan aquí las decisiones reales tomadas durante
M1-M5 (DELETE físico vs. soft delete, ausencia deliberada de
autenticación, búsqueda ILIKE vs. full-text, alcance del servidor MCP),
con sus alternativas descartadas y su justificación. Antes de documentar
cualquiera como ADR, se verifica si `spec.md`/`requirements.md` ya
fijaban la solución (en cuyo caso es una restricción recibida, no una
decisión) — mismo criterio adoptado en la retrospectiva del catálogo 2/5
y ya aplicado en `03_SaaS`.
