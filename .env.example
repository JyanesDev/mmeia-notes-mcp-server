# Copy to .env (gitignored) and fill in real values before `docker compose up`.
# See despliegue.md for the full inventory and which values are secrets.

POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
POSTGRES_DB=notes
APP_PORT=8000
DATABASE_URL=postgresql+psycopg://postgres:changeme@db:5432/notes
